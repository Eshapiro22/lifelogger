{
  "name": "sf-account-cleanup",
  "version": "0.1.0",
  "private": true,
  "description": "Crawl a Salesforce account list, flag duplicates / acquired / defunct / bad-data accounts, and draft + submit Sales Technologies data-quality requests after human review.",
  "type": "module",
  "bin": { "sf-cleanup": "./src/cli.mjs" },
  "scripts": {
    "scan": "node src/cli.mjs scan",
    "research": "node src/cli.mjs research",
    "review": "node src/cli.mjs review",
    "submit": "node src/cli.mjs submit",
    "test": "node --test test/*.test.mjs"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.127.0",
    "playwright": "^1.50.0",
    "zod": "^4.0.0"
  }
}
